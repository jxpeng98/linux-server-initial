# config.env - Linux 服务器初始化预设配置
# Linux Server Initialization Preset Configuration
#
# 将此文件放在脚本同目录下，运行脚本时会自动加载
# Place this file in the same directory as the scripts, it will be auto-loaded
#
# 取消注释并修改需要预设的选项
# Uncomment and modify the options you want to preset

# ============================================
# 语言设置 / Language Settings
# ============================================
# INIT_LANG="zh"                    # zh=中文, en=English

# ============================================
# 用户设置 / User Settings (setup_env.sh)
# ============================================
# NEW_USER="dev"                    # 要创建的用户名 / Username to create
# SKIP_USER_CREATION="n"            # 跳过用户创建 (y/n) / Skip user creation
# SUDO_NOPASSWD="n"                 # sudo 免密码 (y/n) / Passwordless sudo
# SKIP_BASE_PACKAGES="n"            # 跳过基础包安装 (y/n) / Skip base packages
# SKIP_SSH_KEY="n"                  # 跳过 SSH 公钥配置 (y/n) / Skip SSH key config
# SSH_PUB_KEY=""                    # SSH 公钥内容 / SSH public key content
# LOCK_USER_PASSWORD="y"            # 锁定用户密码 (y/n) / Lock user password
# SKIP_ZSH_SETUP="n"                # 跳过 Zsh/Oh My Zsh 安装 (y/n) / Skip Zsh setup
# INSTALL_MISE="y"                  # 安装 mise (y/n) / Install mise
# CONTAINER_CHOICE="1"              # 容器运行时: 1=Docker, 2=Podman, 0=跳过

# ============================================
# SSH/防火墙设置 / SSH/Firewall Settings (setup_ssh_firewall.sh)
# ============================================
# SSH_PORT="2222"                   # 新 SSH 端口 / New SSH port
# ALLOW_FORWARDING="y"              # 允许端口转发 (y/n) / Allow port forwarding
# ALLOW_USERS=""                    # SSH 用户白名单，逗号分隔 / Allowed users (comma-separated)
# ENABLE_UFW="y"                    # 启用 UFW 防火墙 (y/n) / Enable UFW
# INSTALL_FAIL2BAN="y"              # 安装 fail2ban (y/n) / Install fail2ban
# DISABLE_22="n"                    # 禁用 22 端口 (y/n) / Disable port 22

# ============================================
# 安全加固设置 / Security Hardening (setup_security_hardening.sh)
# ============================================
# SETUP_SYSCTL="y"                  # 配置内核参数 (y/n) / Configure sysctl
# DISABLE_IPV6="n"                  # 禁用 IPv6 (y/n) / Disable IPv6
# SETUP_AUTO_UPDATES="y"            # 配置自动更新 (y/n) / Setup auto-updates
# SETUP_AUDITD="y"                  # 安装配置 auditd (y/n) / Setup auditd
# SETUP_LOGIN_SECURITY="y"          # 配置登录安全 (y/n) / Setup login security
# DISABLE_RISKY_SERVICES="y"        # 禁用危险服务 (y/n) / Disable risky services
# HARDEN_FILE_PERMISSIONS="y"       # 加固文件权限 (y/n) / Harden file permissions
# INSTALL_SECURITY_TOOLS="n"        # 安装安全工具 (y/n) / Install security tools

# ============================================
# 全局设置 / Global Settings
# ============================================
# INIT_SKIP_CHECKSUM="0"            # 跳过完整性校验 (1=跳过) / Skip checksum (1=skip)
# INIT_NON_INTERACTIVE="0"          # 非交互模式 (1=启用) / Non-interactive mode (1=enable)
